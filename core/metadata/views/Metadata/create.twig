{% extends "Templates/Layouts/inside.twig" %}

{% block layoutcontent %}

    <div class="page-title">
        <div class="title-env">
            <h1 class="title">{% if current_submenu != 'metadata_list' %}{{ "breadcrumbMetadataCreate"|i18n }}{% else %}{{ "breadcrumbMetadataUpdate"|i18n }}{% endif %}</h1>
            <p class="description"></p>
        </div>
        <div class="breadcrumb-env">
            <ol class="breadcrumb bc-1">
                <li>
                    <a href="{{ ROOT }}"><i class="fa-home"></i>Főoldal</a>
                </li>
                <li>
                    {{ ('menu' ~ 'menu.metadata.top'|config|capitalize)|i18n }}
                </li>
                <li class="active">
                    <strong>{% if current_submenu != 'metadata_list' %}{{ "breadcrumbMetadataCreate"|i18n }}{% else %}{{ "breadcrumbMetadataUpdate"|i18n }}{% endif %}</strong>
                </li>
            </ol>
        </div>
    </div>

    {% form.open url('metadataAjax',{actiontarget: 'metadata', maintarget: 'save'}),{'method':'post','id':'metadata-Form'} %}
    {{ forms.build(form) }}
    </form>

{% endblock %}