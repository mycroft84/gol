<?php
/**
 * User: Tibi
 * Date: {{ 'now'|date('Y-m-d') }}
 * Time: {{ 'now'|date('H:i') }}
 */
class {{ classname }} extends ORM
{
    protected $_table_name = '{{ table_name }}';
    protected $_primary_key = '{{ primary_key }}';
    protected $is_translate_orm = true;

}